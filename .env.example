# Pauseward web — copy to .env.local and adjust for your environment.
# Restart `npm run dev` after changing this file.

# pauseward-api base URL (no trailing slash)
NEXT_PUBLIC_PAUSEWARD_API_URL=http://localhost:8080
